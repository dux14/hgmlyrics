// Forced alignment (WhisperX en Modal) — disparado al confirmar la subida del
// mp3 completo de una cancion. Stub de la task A2; la task A3 lo reemplaza por
// el dispatch real (encolar/llamar Modal y persistir song_line_timings).
// TODO-A3: implementar dispatchAlign.
export async function dispatchAlign(_songId) {}
