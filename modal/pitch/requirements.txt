torch==2.4.1
torchaudio==2.4.1
audio-separator[gpu]==0.28.5
demucs==4.0.1
huggingface_hub==0.25.2
httpx==0.27.2
soundfile==0.12.1
librosa==0.10.2
numpy==1.26.4
fastapi[standard]==0.115.6
pyphen==0.15.0
# whisperx 3.3.1: la 3.1.5 pinnea faster-whisper==1.0.1 -> av==11.* (sin wheel en
# el mirror de Modal, que solo trae av>=12; compilar PyAV 11 falla por pkg-config
# + ffmpeg 6). 3.3.1 usa faster-whisper==1.1.0 (av>=11 -> wheel de av 12) y mantiene
# torch>=2 (compatible con torch==2.4.1) + pyannote.audio==3.3.2 (mismo que el
# Estudio). La 3.3.4+ exige torch>=2.5.1 y rompe el pin. API de lyrics.py sin cambios.
whisperx==3.3.1
pyloudnorm==0.1.1
torchcrepe==0.0.23
git+https://github.com/asteroid-team/asteroid
# ── M2 render/export (CPU puro, sin GPU) ─────────────────────────────────────
cairosvg==2.7.1
pretty_midi==0.2.10
music21==9.1.0
# ── M5 coro opcional (flag PITCH_CHOIR, choir_basicpitch.py) ─────────────────
# ADVERTENCIA sin resolver (gate del usuario en `modal deploy`): en PyPI,
# basic-pitch==0.4.0 declara tensorflow<2.15.1 como dependencia INCONDICIONAL
# para Linux+python>=3.11 (no esta gateada detras de un extra) — instalar
# CUALQUIER variante de basic-pitch en esta imagen (debian_slim python 3.11)
# arrastra tensorflow igual, con riesgo real de choque de pines con el stack
# torch==2.4.1/whisperx/av ya presente. El extra [onnx] no evita esa instalacion
# de TF (verificado leyendo requires_dist del paquete), pero SI trae onnxruntime
# para que choir_basicpitch.py pueda pedir explicitamente el modelo .onnx
# empaquetado (build_icassp_2022_model_path(FilenameSuffix.onnx)) en vez del
# SavedModel de TF que basic_pitch elegiria por defecto (TF instalado => TF
# tiene prioridad en su orden interno TF > CoreML > TFLite > ONNX). Esto reduce
# el uso en runtime de TF, no el riesgo de instalacion — ese riesgo se valida
# solo en el smoke real de `modal deploy`.
basic-pitch[onnx]==0.4.0
